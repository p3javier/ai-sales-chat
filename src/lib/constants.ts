export enum EVariant {
  sent = "sent",
  received = "received",
  loading = "loading",
}

export enum EVideoError {
  svidNotFound = '"svid" param not found',
  notFound = "Video not found",
  noError = "",
}
